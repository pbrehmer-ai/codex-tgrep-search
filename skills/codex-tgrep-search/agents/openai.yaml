interface:
  display_name: "Codex tgrep Search"
  short_description: "Efficient text search across large codebases"
  default_prompt: "Use $codex-tgrep-search to locate the relevant code and inspect current files efficiently."
policy:
  allow_implicit_invocation: true
